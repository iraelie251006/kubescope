apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: orderflow
spec:
  type: ClusterIP
  selector:
    app: postgres-depl
    tier: db
  ports:
   - port: 5432
     targetPort: 5432
